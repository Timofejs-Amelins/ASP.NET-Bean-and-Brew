@model IEnumerable<Beanbrew.Models.Coffee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CoffeeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnitPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CoffeeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnitPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CoffeeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CoffeeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CoffeeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
